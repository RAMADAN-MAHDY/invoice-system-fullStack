<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>ملفات الإكسل</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet" />
</head>
<body class="bg-gray-100 min-h-screen">
  <nav class="bg-white shadow p-4 flex justify-between items-center">
    <span class="font-bold text-xl">لوحة فواتير قطع الغيار</span>
    <form method="POST" action="/logout">
      <button class="bg-red-500 text-white px-4 py-2 rounded">تسجيل الخروج</button>
    </form>
    <form method="GET" action="/dashboard">
      <button class="bg-green-500 text-white px-4 py-2 rounded">الرئيسيه</button>
    </form>
    <form method="GET" action="/sales">
      <button class="bg-green-500 text-white px-4 py-2 rounded">المبيعات</button>
    </form>
  </nav>

  <div class="max-w-4xl mx-auto bg-white shadow-md rounded-lg mt-10 p-6">
    <h1 class="text-2xl font-bold text-center mb-6">📊 ملفات الإكسل المحفوظة</h1>

    <% if (files.length === 0) { %>
      <p class="text-center text-gray-500">لا توجد ملفات إكسل محفوظة.</p>
    <% } else { %>
      <div class="overflow-x-auto">
        <table class="min-w-full bg-white border border-gray-200">
          <thead>
            <tr>
              <th class="py-2 px-4 border-b">اسم الملف</th>
              <th class="py-2 px-4 border-b">تاريخ الإنشاء</th>
              <th class="py-2 px-4 border-b">إجراءات</th>
            </tr>
          </thead>
          <tbody>
            <% files.forEach(file => { %>
              <tr class="hover:bg-gray-50">
                <td class="py-2 px-4 border-b">sales_<%= file.createdAt.toISOString().slice(0,10) %>.xlsx</td>
                <td class="py-2 px-4 border-b"><%= file.createdAt.toLocaleString() %></td>
                <td class="py-2 px-4 border-b flex space-x-2 justify-center">
                  <a href="/api/items/download/<%= file._id %>" class="bg-blue-500 text-white px-3 py-1 rounded hover:bg-blue-600">⬇️ تحميل</a>
                  <form action="/excel-files/delete/<%= file._id %>" method="POST" onsubmit="return confirm('هل أنت متأكد من حذف هذا الملف؟');">
                    <button type="submit" class="bg-red-500 text-white px-3 py-1 rounded hover:bg-red-600">🗑️ حذف</button>
                  </form>
                </td>
              </tr>
            <% }); %>
          </tbody>
        </table>
      </div>
    <% } %>
  </div>
</body>
</html>