<%- include('partials/header') %>

<div class="container mx-auto p-4">
    <div class="glass p-6 rounded-lg shadow-lg">
        <h1 class="text-3xl font-bold text-center mb-6 text-white">📊 ملفات الإكسل المحفوظة</h1>

        <% if (files.length === 0) { %>
            <p class="text-center text-gray-400">لا توجد ملفات إكسل محفوظة.</p>
        <% } else { %>
            <div class="overflow-x-auto">
                <table class="min-w-full">
                    <thead class="bg-gray-800">
                        <tr>
                            <th class="p-3 text-center">اسم الملف</th>
                            <th class="p-3 text-center">تاريخ الإنشاء</th>
                            <th class="p-3 text-center">إجراءات</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% files.forEach(file => { %>
                            <tr class="border-b border-gray-800 hover:bg-gray-700">
                                <td class="p-3 text-center">sales_<%= file.createdAt.toISOString().slice(0,10) %>.xlsx</td>
                                <td class="p-3 text-center"><%= file.createdAt.toLocaleString() %></td>
                                <td class="p-3 flex space-x-2 rtl:space-x-reverse justify-center">
                                    <a href="/api/items/download/<%= file._id %>" class="bg-blue-500 text-white px-3 py-1 rounded-lg hover:bg-blue-600 transition">⬇️ تحميل</a>
                                    <form action="/excel-files/delete/<%= file._id %>" method="POST" onsubmit="return confirm('هل أنت متأكد من حذف هذا الملف؟');">
                                        <button type="submit" class="bg-red-500 text-white px-3 py-1 rounded-lg hover:bg-red-600 transition">🗑️ حذف</button>
                                    </form>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        <% } %>
    </div>
</div>

<%- include('partials/footer') %>