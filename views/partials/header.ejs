<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>لوحة التحكم</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="https://cdn.jsdelivr.net/gh/alpinejs/alpine@v2.x.x/dist/alpine.min.js" defer></script>
    <style>
        .glass {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        .drawer-content {
            transition: transform 0.3s ease-in-out;
        }
    </style>
</head>
<body class="bg-gray-900 text-gray-200 font-sans">
    <div x-data="{ drawerOpen: false }">
        <!-- Navbar -->
        <header class="glass fixed top-0 left-0 right-0 z-20">
            <div class="container mx-auto px-4">
                <div class="flex justify-between items-center py-4">
                    <span class="font-bold text-xl text-white drawer-content glass p-2 rounded-xl ">     RAMADAN WEB </span>
                    
                    <!-- Desktop Nav -->
                    <nav class="hidden md:flex items-center gap-4">
                        <a href="/dashboard" class="text-gray-300 hover:text-white transition">الرئيسية</a>
                        <a href="/sales" class="text-gray-300 hover:text-white transition">المبيعات</a>
                        <a href="/profit" class="text-gray-300 hover:text-white transition">المشتريات والمبيعات</a>
                        <a href="/expenses" class="text-gray-300 hover:text-white transition">المصروفات</a>
                        <!-- <a href="/excel-files" class="text-gray-300 hover:text-white transition">ملفات الإكسل</a> -->
                        <form method="POST" action="/logout">
                            <button class="bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-lg transition">تسجيل الخروج</button>
                        </form>
                    </nav>

                    <!-- Mobile Nav Toggle -->
                    <div class="md:hidden">
                        <button @click="drawerOpen = true" class="text-white focus:outline-none">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path></svg>
                        </button>
                    </div>
                </div>
            </div>
        </header>

        <!-- Side Drawer -->
        <div @click.away="drawerOpen = false" class="fixed inset-0 z-30 md:hidden" x-show="drawerOpen" style="display: none;">
            <!-- Overlay -->
            <div class="fixed inset-0 bg-black opacity-50" @click="drawerOpen = false"></div>
            
            <!-- Drawer Content -->
            <div class="drawer-content fixed top-0 right-0 h-full w-64 glass shadow-lg p-6" :class="{ 'translate-x-0': drawerOpen, 'translate-x-full': !drawerOpen }">
                <nav class="flex flex-col gap-6 mt-8">
                    <a href="/dashboard" class="text-lg text-gray-200 hover:text-white transition">الرئيسية</a>
                    <a href="/sales" class="text-lg text-gray-200 hover:text-white transition">المبيعات</a>
                    <a href="/profit" class="text-lg text-gray-200 hover:text-white transition">المشتريات والمبيعات</a>
                    <a href="/expenses" class="text-lg text-gray-200 hover:text-white transition">المصروفات</a>
                    <!-- <a href="/excel-files" class="text-lg text-gray-200 hover:text-white transition">ملفات الإكسل</a> -->
                    <form method="POST" action="/logout" class="mt-auto">
                        <button class="w-full bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-lg transition">تسجيل الخروج</button>
                    </form>
                </nav>
            </div>
        </div>

        <main class="pt-24 container mx-auto px-4">
            <!-- Page content will be injected here -->